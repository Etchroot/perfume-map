# Firebase 서비스 계정 키 (JSON 전체를 한 줄 문자열로 붙여넣기)
# 발급 방법: Firebase Console → 프로젝트 설정 → 서비스 계정 → 새 비공개 키 생성
# JSON 파일을 열고, 내용 전체를 아래 값 자리에 복사하세요.
FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"YOUR_PROJECT_ID",...}
